@namespace Masa.Stack.Components.UserCenters
@inherits MasaComponentBase

<SDefaultModal @bind-Value="_visible"
              Title="@T("IdentityCardAuth")"
              FormModel="IdCardAuth"
              OnCancel="HandleOnCancel"
              OnSave="HandleOnOk">
    <MRow>
        <MCol Cols="12">
            <SDefaultTextField @bind-Value="@IdCardAuth.Name"
                              Label="@T("RealName")">
            </SDefaultTextField>
        </MCol>
        <MCol Cols="12">
            <SDefaultTextField @bind-Value="@IdCardAuth.Number"
                              Label="@("IdentityCardNumber")">
            </SDefaultTextField>
        </MCol>
        <MCol Cols="12">TODO: date</MCol>
        <MCol Cols="12">TODO: 详情地址</MCol>
        <MCol Cols="12">TODO: Images</MCol>
    </MRow>
</SDefaultModal>

@code {

    [Parameter]
    public EventCallback OnFinish { get; set; }

    private bool _visible;

    private IdentityCardAuthentication IdCardAuth { get; set; } = new();

    internal void Open()
    {
        _visible = true;
        StateHasChanged();
    }

    private void HandleOnCancel()
    {
        IdCardAuth = new();
        _visible = false;
    }

    private async Task HandleOnOk()
    {
        // TODO: id card save
        
        // success
        await OnFinish.InvokeAsync();
    }

}
