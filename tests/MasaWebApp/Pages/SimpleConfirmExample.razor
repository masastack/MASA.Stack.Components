@page "/simpleConfirmExample"
@using Masa.Stack.Components.Extensions;
@using Masa.Stack.Components.Shared.IntegrationComponents.Confirm;

@code {
    [Inject]
    public IPopupService PopupService { get; set; } = default!;


    protected override async Task OnAfterRenderAsync(bool firstRender)
    {

        if (firstRender)
        {
            var confirm = await PopupService.SimpleConfirmAsync("非常长自动哦你的内容删除1111111111111111delete-testaaaaaaa自动换行测试");
            if (confirm is true)
            {
                
            }
        }
        await base.OnAfterRenderAsync(firstRender);
    }
}